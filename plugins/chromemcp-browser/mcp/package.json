{
  "name": "chromemcp",
  "version": "0.1.0",
  "private": true,
  "description": "Project-local MCP server stack. Runs Playwright MCP as a long-running HTTP/SSE service connected to ChromeMCP's bridged Chrome via CDP, so any number of MCP clients in WSL can share the same browser session.",
  "engines": {
    "node": ">=18.18"
  },
  "dependencies": {
    "@playwright/mcp": "0.0.75"
  },
  "overrides": {
    "playwright-core": "1.60.0",
    "playwright": "1.60.0"
  },
  "scripts": {
    "start": "bash ./start.sh",
    "stop": "bash ./stop.sh"
  }
}
